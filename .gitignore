node_modules/
.next/
out/
dist/
build/
.env
.env.local
.env*.local
!.env.example
*.log
.DS_Store
.vercel
.turbo
coverage/
.pytest_cache/
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Local model weights — never commit
models/
*.gguf
*.safetensors
*.bin
*.pt

# Local data caches — large or generated
data/cms/raw/
data/cms/parquet/
data/cms/*.duckdb
data/cms/*.duckdb.wal
data/synthea/output/
data/ledger/*.jsonl

# IDE
.idea/
.vscode/

# Submission scratch
.scratch/
.video-rough/

# Demo recording intermediates — keep healthpulse-edge-demo.mp4, ignore the
# raw screen-capture takes (regenerable via scripts/record-take.sh) and the
# dry voiceover input (already muxed into the final MP4).
assets/recordings/*.webm
assets/recordings/take-*.mov
assets/recordings/voiceover-*.m4a
assets/recordings/frame-*.png
RESUME.md
resume.md
